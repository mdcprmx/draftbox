Конспект тестов типо:
- модульные тесты это юнит-тесты. одно и то же
- arrange, act, assert. 
-- начальные входные данные которые подставляются
-- вызываем функцию и тестируем её
-- проверяем на корректность. (с краевыми значениеями)
--  
--





C FILE SIMULATION
________________
#include <stdio.h>
#include <stdlib.h>
#include <check.h>

START_TEST(test_random_funct_name)
{
// Arrange (инициализируем)
struct tests test_funct_name = (tests*) malloc (sizeof(test));
int kraevoe_znachenie1 = 0;
char *stringa = "boiiii dance dance";

// Act (запускаем тесты)
s21_funct_name(stringa, kraevoe_znachenie1, n);

// Assert (проверяем)
ck_assert_int_eq(test_funct_name->doesStuffExist, 0);
free(test_funct_name);

}
END_TEST

Suite *function_that_returns_suite(void)
{
Suite returning_suite;
TCase *test_case_one, *test_case_two; // итак далее

returning_suite = suite_create("")


}


 
